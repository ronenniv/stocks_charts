import 'package:flutter/material.dart';

const kMaxCharCompanyName = 20;

// stock_tile
const kStockSymbolTextStyle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 40.0);
const kLastPriceTextStyle =
    TextStyle(fontSize: 40.0, fontWeight: FontWeight.w300);
const kCompanyNameTextStyle =
    TextStyle(fontSize: 20.0, fontWeight: FontWeight.w300);

// add_stock_page
const kAddStockAppBarText = Text('Add New Stock');
const kSubmitButtonTextStyle = TextStyle(fontSize: 40.0, color: Colors.white);
const String kErrorMessageText = 'Error: Symbol not found';
const String kHintText = 'Enter Symbol';

// chart_page
const kChartAppBarText = Text('Stock Chart');

// home_page
const kHomeAppBarText = Text('Stock Quotes');
